[pytest]
testpaths = tests/unit
markers =
    requires_torch: tests that load torch/transformers (incompatible with faiss in same process on Windows)
    requires_faiss: tests that use faiss search (incompatible with torch in same process on Windows)
